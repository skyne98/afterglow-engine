[package]
name = "afterglow-basis-encoder"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false
description = "Offline-only Basis Universal UASTC encoder for afterglow-pipeline"

[dependencies]
basis-universal = "0.3.1"

[dev-dependencies]
afterglow-texture = { path = "../afterglow-texture" }
