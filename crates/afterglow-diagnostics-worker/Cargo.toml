[package]
name = "afterglow-diagnostics-worker"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Native RPC adapter for bounded diagnostics capture"

[target.'cfg(unix)'.dependencies]
afterglow-rpc = { path = "../afterglow-rpc" }
afterglow-rpc-macros = { path = "../afterglow-rpc-macros" }
afterglow-telemetry = { path = "../afterglow-telemetry", features = ["collector"] }
getrandom = "0.3"
serde_json.workspace = true
