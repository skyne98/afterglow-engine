[package]
name = "afterglow-meshopt"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false
description = "Rust + WASM wrappers for meshoptimizer — mesh optimization, LOD, compression, meshlets"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]

[build-dependencies]
cc = "1"
